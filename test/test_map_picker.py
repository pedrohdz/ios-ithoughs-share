# pylint: disable=missing-docstring,redefined-outer-name
import os
# from unittest import mock
# import pytest

from ithoughts_notes import MapPicker


# -----------------------------------------------------------------------------
# Fixtures
# -----------------------------------------------------------------------------
# @pytest.fixture
# def fixed_created():
#     return datetime(2001, 6, 25, 17, 53, 33, 78, tzinfo=TZ)


# -----------------------------------------------------------------------------
# MindMaps
# -----------------------------------------------------------------------------
def test_mind_maps_dump(tmpdir):
    #tmpfile = os.path.join(tmpdir, 'mind_maps.json')
    ## with open(tmpfile, 'w') as outfile:
    ##     mind_maps.dump(outfile, sort_keys=True, indent=2)
    ## with open(tmpfile, 'r') as infile:
    ##     what_was_writter = infile.read()
    ## assert what_was_writter == mind_maps_json
    #picker = MapPicker()
    #picker.open_maps(tmpfile)
    pass
